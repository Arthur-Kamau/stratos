project {
  name = "expect"
  version = "1.0.0"
  
  authors = ["Your Name"]
  description = "Demonstration of loop patterns in Stratos"
}


build {
  entry = src/main.st
  output = build/expect
}

dependencies {
  stratos = ">=1.0.0"
}