project {
  name = sorting_no_gc
  version = "1.0.0"
  description = "Sorting algorithms example without garbage collection"
}

build {
  entry = src/main.st
  output = build/sorting
}

memory {
  gc = false
}
