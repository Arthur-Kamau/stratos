project {
  name = colors-demo
  version = "1.0.0"
  description = "Demo project showcasing stratos-colors dependency"
  authors = ["Stratos Examples"]
}

build {
  entry = src/main.st
  output = build/colors-demo
}

dependencies = [
  {
    name = colors
    url = "../../external/stratos-colors"
  }
]
