package util;

// the util package must match the parent folder name
// unless in root folder(ie under src)
class Util{
   pub fn hello(){
         println("util hello");
     }
}

struct UtilStruct{
   x: i32,
   y: i32
}