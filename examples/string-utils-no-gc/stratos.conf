project {
  name = string_utils_no_gc
  version = "1.0.0"
  description = "String utilities example without garbage collection"
}

build {
  entry = src/main.st
  output = build/string_utils
}

memory {
  gc = false
}
