project {
  name = calculator_no_gc
  version = "1.0.0"
  description = "Simple calculator example without garbage collection"
}

build {
  entry = src/main.st
  output = build/calculator
}

memory {
  gc = false
}
