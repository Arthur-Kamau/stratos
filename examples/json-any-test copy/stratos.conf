project {
  name = data-convert
  version = "1.0.0"
  description = "A new Stratos project"
  author = "Your Name"
  license = "MIT"
}

build {
  entry = src/main.st
  output = build/data-convert
}

dependencies {
  # Add dependencies here
  # example = "libs/example"
}
