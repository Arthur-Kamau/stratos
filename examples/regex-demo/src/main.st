package main;

fn main() {
    println("Stratos Regex Module - Complete Demo Suite");
    println("==========================================");

    // 1. Run Simple Tests (Basic Functionality)
    runSimpleTest();
    println("\n" + "-".repeat(60) + "\n");

    // 2. Run Validation Demo (Email, URL, Numbers)
    runValidationDemo();
    println("\n" + "-".repeat(60) + "\n");

    // 3. Run Features Demo (Comprehensive feature showcase)
    runFeaturesDemo();
    println("\n" + "-".repeat(60) + "\n");

    // 4. Run Text Processing Demo (Real-world use cases)
    runTextProcessingDemo();

    println("\n" + "=".repeat(60));
    println("All Regex Demos Completed Successfully");
    println("=".repeat(60));
}

    println("==========================================");

    // 1. Run Simple Tests (Basic Functionality)
    runSimpleTest();
    println("\n" + "-".repeat(60) + "\n");

    // 2. Run Validation Demo (Email, URL, Numbers)
    runValidationDemo();
    println("\n" + "-".repeat(60) + "\n");

    // 3. Run Features Demo (Comprehensive feature showcase)
    runFeaturesDemo();
    println("\n" + "-".repeat(60) + "\n");

    // 4. Run Text Processing Demo (Real-world use cases)
    runTextProcessingDemo();

    println("\n" + "=".repeat(60));
    println("All Regex Demos Completed Successfully");
    println("=".repeat(60));
}