project {
  name = fibonacci_no_gc
  version = "1.0.0"
  description = "Fibonacci sequence example without garbage collection"
}

build {
  entry = src/main.st
  output = build/fibonacci
}

memory {
  gc = false
}
