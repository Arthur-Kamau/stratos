package main;

enum Color {
    RED,
    GREEN,
    BLUE
}

fn main() {
    println("--- Enum Demo ---");
    
    // Enum usage
    // Assuming enums are accessed like types or integers
    // or Color.RED if scoped
    
    println("Enum defined successfully.");
    
    // Note: Printing enum values depends on runtime support
    // val c = Color.RED;
}
