project {
  name = primes_no_gc
  version = "1.0.0"
  description = "Prime numbers example without garbage collection"
}

build {
  entry = src/main.st
  output = build/primes
}

memory {
  gc = false
}
