project {
  name = app_config_options
  version = "1.0.0"
  description = "A simple project to show Stratos configuration options"
}

build {
  entry = src/main.st
  output = build/app_config_options
}
