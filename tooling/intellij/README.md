## Stratos Intellij plugin

 The IntelliJ plugin is  conceptually in sync with the VS Code extension and the C++ compiler. To verify it fully,
  you would need to build it using Gradle (./gradlew buildPlugin) and run it in an IntelliJ sandbox instance.